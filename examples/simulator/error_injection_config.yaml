# Mobile Simulator - Error Injection Configuration
# ===================================================
# Configuration for testing server error handling

# Server connection settings
server:
  host: "127.0.0.1"
  port: 8443
  connect_timeout: 30.0
  read_timeout: 30.0
  retry_count: 3
  retry_backoff: [1.0, 2.0, 4.0]

# PSK-TLS credentials
psk:
  identity: "test_card_error"
  key: "0102030405060708090A0B0C0D0E0F10"

# Virtual UICC configuration
uicc:
  iccid: "8901234567890123456"
  imsi: "310150123456789"

  gp:
    version: "2.2.1"
    scp_version: "03"
    isd_aid: "A000000151000000"

# Simulation behavior - error injection mode
behavior:
  mode: "error"
  response_delay_ms: 20

  # Error injection settings
  error:
    rate: 0.2  # 20% of responses will be errors
    codes:
      - "6A82"  # File not found
      - "6985"  # Conditions not satisfied
      - "6D00"  # INS not supported
      - "6A86"  # Incorrect P1P2

  timeout:
    probability: 0.0

  connection:
    mode: "single"
